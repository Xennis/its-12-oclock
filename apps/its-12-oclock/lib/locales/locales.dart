
import 'dart:ui';

class Locales {
  static const Locale de = Locale('de');
  static const Locale en = Locale('en');

  static Map<Locale, String> supported = {
    de: "German",
    en: "English"
  };
}
